import { useParams } from "react-router-dom";

export default function ParcelDetailsScreen() {
  // const item = props.item;

  // const parcel_id = useParams();
  // console.log("parcel id", parcel_id);

  // const parcel = item.find((item) => item.parcel_id === parcel_id);

  // const { id, eta, last_updated, user_name, location_name } = parcel;

  return (
    <div>
      {/* <div>
        <h2>Pacote:</h2>
        <p>{id}</p>
        <p> {user_name}</p>
        <p>{location_name}</p>
        <p>{eta}</p>
        <p>{last_updated}</p>
      </div> */}
      Need to work
    </div>
  );
}
